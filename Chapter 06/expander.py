import streamlit as st

with st.echo():
    with st.expander("How to use Streamlit", icon="👉"):
        st.write("1. Write some Streamlit code.")
        st.write("2. Run your script.")
        st.write("3. See your app in a browser.")
